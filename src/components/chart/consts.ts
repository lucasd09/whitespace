export const chartThemes = { light: "", dark: ".dark" } as const;

export const chartColors = [
  "hsl(var(--chart-1))",
  "hsl(var(--chart-2))",
  "hsl(var(--chart-3))",
  "hsl(var(--chart-4))",
  "hsl(var(--chart-5))",
];

export const chartMargin = {
  top: 5,
  right: 30,
  left: 20,
  bottom: 5,
};
